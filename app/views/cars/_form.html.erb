<%= simple_form_for @car do |f| %>
  <%= f.input :color %>
  <%= f.input :year %>
  <%= f.input :mileage %>
  <%= f.input :description %>
  <%= f.collection_select :manufacturer_id, Manufacturer.all, :id, 
                          :name, prompt: true %><br>
  <%= f.button :submit %>

  <% end %>
